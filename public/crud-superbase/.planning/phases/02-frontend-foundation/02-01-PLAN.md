---
phase: "02-frontend-foundation"
plan: "02-01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "frontend/package.json"
  - "frontend/vite.config.js"
  - "frontend/tailwind.config.js"
  - "frontend/postcss.config.js"
  - "frontend/index.html"
  - "frontend/src/index.css"
  - "frontend/.gitignore"
autonomous: true
requirements: []
must_haves:
  truths:
    - "Vite dev server starts on http://localhost:5173 without error"
    - "Tailwind CSS v3 (not v4) is installed; tailwind.config.js is honored"
    - "Font Awesome 6.5.1 is loaded via CDN <link> in index.html <head>"
    - "Inter font is loaded via Google Fonts CDN <link> in index.html <head>"
    - "Anti-flash IIFE script runs synchronously in index.html <head> BEFORE the React bundle <script type=\"module\"> tag and toggles the 'dark' class on <html> from localStorage 'theme' key (default dark when key absent or not 'light')"
    - ".glass utility class produces blur(12px) + rgba(255,255,255,0.75) in light, rgba(15,23,42,0.75) in dark"
    - "Body shows dual radial gradient (indigo top-left, purple bottom-right at 0.15 alpha)"
    - "Sidebar scrollbar custom style exists (.sidebar-scroll, 6px width, #4f46e5 thumb, 999px radius)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Vite React project manifest pinning tailwindcss@^3 and react-router-dom@^6"
      contains: "\"tailwindcss\": \"^3"
    - path: "frontend/vite.config.js"
      provides: "Vite config with @vitejs/plugin-react"
      min_lines: 4
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind v3 config with darkMode:'class' and content paths"
      contains: "darkMode: 'class'"
    - path: "frontend/postcss.config.js"
      provides: "PostCSS config with tailwindcss + autoprefixer plugins"
      contains: "tailwindcss"
    - path: "frontend/index.html"
      provides: "HTML entry: anti-flash IIFE before module script, FA 6.5.1 CDN, Google Fonts Inter, #root div, /src/main.jsx module script"
      contains: "font-awesome/6.5.1/css/all.min.css"
    - path: "frontend/src/index.css"
      provides: "Tailwind directives + .glass + body radial gradient + .sidebar-scroll styles"
      contains: "@tailwind base"
  key_links:
    - from: "frontend/index.html"
      to: "localStorage 'theme' key + document.documentElement.classList"
      via: "anti-flash IIFE inside <script> tag, placed BEFORE <script type=\"module\" src=\"/src/main.jsx\">"
      pattern: "localStorage\\.getItem\\(['\"]theme['\"]\\)"
    - from: "frontend/index.html"
      to: "frontend/src/main.jsx"
      via: "<script type=\"module\" src=\"/src/main.jsx\"> at end of <body>"
      pattern: "src=\"/src/main\\.jsx\""
    - from: "frontend/src/index.css"
      to: "frontend/tailwind.config.js"
      via: "@tailwind base/components/utilities directives processed by postcss + tailwindcss plugin"
      pattern: "@tailwind (base|components|utilities)"
---

# Plan 02-01: Frontend Scaffold + Tailwind v3 + index.html Shell

## Objective

Scaffold the Vite + React project at `frontend/`, install and pin Tailwind CSS v3, configure PostCSS, write `index.html` with the anti-flash IIFE + FA + Inter CDN links, and author `src/index.css` with the `.glass` utility, body radial gradient, and `.sidebar-scroll` styles — all transcribed exactly from `UX-UI/index.html`.

**Purpose:** Establish the foundation upon which Plans 02-02 (stores + routing) and 02-03 (components + pages) are built. This plan delivers a runnable Vite project that boots in dark mode with no flash and renders the Tailwind base layer.

**Output:** A `frontend/` directory whose `npm run dev` serves on `http://localhost:5173` and shows the dual-gradient body background with the `dark` class already on `<html>`.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-foundation/02-CONTEXT.md
@.planning/phases/02-frontend-foundation/02-RESEARCH.md
@.planning/phases/02-frontend-foundation/02-PATTERNS.md
@UX-UI/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React project and pin dependencies</name>
  <files>frontend/package.json, frontend/vite.config.js, frontend/.gitignore</files>
  <read_first>
    - .planning/phases/02-frontend-foundation/02-RESEARCH.md (Standard Stack table + Installation block + Pitfall 1 — Tailwind v4 must be avoided)
    - .planning/phases/02-frontend-foundation/02-CONTEXT.md (D-05 frontend path, D-06 Tailwind v3 lock)
    - .planning/phases/02-frontend-foundation/02-PATTERNS.md (vite.config.js pattern)
    - backend/package.json (mirror style: scripts naming, no extraneous deps)
  </read_first>
  <action>
    From the repository root, run `npm create vite@latest frontend -- --template react` to scaffold (D-05). Then in `frontend/`, run `npm install` to install scaffold defaults. Next install dev deps with explicit pin: `npm install -D tailwindcss@^3 postcss autoprefixer` — the `@^3` is mandatory per D-06 because the npm default for `tailwindcss` resolves to v4 which uses a different config model and breaks `tailwind.config.js`. Install runtime deps with explicit pins: `npm install zustand react-router-dom@^6` — pin `react-router-dom@^6` because npm default installs v7 which is a major version bump. After install, verify the resulting `package.json` `devDependencies.tailwindcss` starts with `^3.` (NOT `^4.`) and `dependencies.react-router-dom` starts with `^6.` (NOT `^7.`). Do NOT install any `@fortawesome/*` package — Font Awesome is CDN-only per D-04. Ensure `vite.config.js` uses `defineConfig({ plugins: [react()] })` from the scaffold output — leave it untouched after scaffold. Confirm `.gitignore` (scaffold-generated) ignores `node_modules` and `dist`.
  </action>
  <verify>
    <automated>cd frontend && grep -E '"tailwindcss": "\^3' package.json && grep -E '"react-router-dom": "\^6' package.json && test -f vite.config.js && test -f .gitignore && ! grep -q '@fortawesome' package.json</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/package.json` exists with `devDependencies.tailwindcss` matching `^3.\d+` and `dependencies.react-router-dom` matching `^6.\d+`
    - `frontend/package.json` does NOT contain any `@fortawesome/*` entry
    - `frontend/package.json` `dependencies` includes `zustand`, `react`, `react-dom`, `react-router-dom`
    - `frontend/package.json` `devDependencies` includes `tailwindcss`, `postcss`, `autoprefixer`, `vite`, `@vitejs/plugin-react`
    - `frontend/vite.config.js` exists and exports a `defineConfig({ plugins: [react()] })` config (import from `vite` and `@vitejs/plugin-react`)
    - `frontend/node_modules/` exists (install succeeded)
    - `cd frontend && npm run dev -- --port 5173` starts a Vite dev server (terminate after verification)
  </acceptance_criteria>
  <done>Vite React project exists at `frontend/`, Tailwind v3 pinned, React Router v6 pinned, no FA npm package, dev server boots.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v3 (tailwind.config.js + postcss.config.js + src/index.css)</name>
  <files>frontend/tailwind.config.js, frontend/postcss.config.js, frontend/src/index.css</files>
  <read_first>
    - .planning/phases/02-frontend-foundation/02-RESEARCH.md (Pattern 1 — Tailwind v3 Config)
    - .planning/phases/02-frontend-foundation/02-PATTERNS.md (tailwind.config.js / postcss.config.js / src/index.css patterns)
    - .planning/phases/02-frontend-foundation/02-CONTEXT.md (D-06 content paths, D-07 dark class on html, specifics block — exact CSS values)
    - UX-UI/index.html (lines 39-75 — exact values for body radial gradient, .glass, .sidebar-scroll; these are the source of truth)
    - frontend/package.json (verify Tailwind v3 is installed before configuring)
  </read_first>
  <action>
    Write `frontend/tailwind.config.js` using ES module export (`export default { ... }`) with these EXACT keys: `darkMode: 'class'` (per D-07), `content: ['./index.html', './src/**/*.{js,jsx}']` (per D-06), `theme: { extend: {} }`, `plugins: []`. Do NOT use CommonJS `module.exports` — Vite scaffolds use ES modules (`"type": "module"` in package.json). Write `frontend/postcss.config.js` using `export default { plugins: { tailwindcss: {}, autoprefixer: {} } }`. Replace the scaffold-generated `frontend/src/index.css` (delete any default Vite boilerplate) with three Tailwind directives at the top — `@tailwind base; @tailwind components; @tailwind utilities;` — followed by these exact CSS blocks transcribed from `UX-UI/index.html` lines 51-74: (a) `.glass { backdrop-filter: blur(12px); background: rgba(255, 255, 255, 0.75); }`, (b) `.dark .glass { background: rgba(15, 23, 42, 0.75); }`, (c) `body { background: radial-gradient(circle at top left, rgba(99,102,241,.15), transparent 30%), radial-gradient(circle at bottom right, rgba(168,85,247,.15), transparent 30%); }`, (d) `.sidebar-scroll::-webkit-scrollbar { width: 6px; }` and `.sidebar-scroll::-webkit-scrollbar-thumb { background: #4f46e5; border-radius: 999px; }`. Add `* { font-family: 'Inter', sans-serif; }` (from UX-UI line 48). Do NOT add any additional utility classes — the rest is handled by Tailwind. Do NOT use `style={{ backdropFilter: ... }}` anywhere (see Pitfall 4).
  </action>
  <verify>
    <automated>cd frontend && grep -q "darkMode: 'class'" tailwind.config.js && grep -q "'./index.html'" tailwind.config.js && grep -q "'./src/\*\*/\*\.{js,jsx}'" tailwind.config.js && grep -q "tailwindcss" postcss.config.js && grep -q "autoprefixer" postcss.config.js && grep -q "@tailwind base" src/index.css && grep -q "@tailwind components" src/index.css && grep -q "@tailwind utilities" src/index.css && grep -q "backdrop-filter: blur(12px)" src/index.css && grep -q "rgba(15, 23, 42, 0.75)" src/index.css && grep -q "radial-gradient(circle at top left" src/index.css && grep -q "#4f46e5" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/tailwind.config.js` exports default with `darkMode: 'class'` literal string match
    - `frontend/tailwind.config.js` `content` array contains both `'./index.html'` AND `'./src/**/*.{js,jsx}'` (exact strings)
    - `frontend/postcss.config.js` `plugins` object includes both `tailwindcss` and `autoprefixer` keys
    - `frontend/src/index.css` first three non-comment lines are the three `@tailwind` directives in order: base, components, utilities
    - `frontend/src/index.css` contains literal substrings: `backdrop-filter: blur(12px)`, `rgba(255, 255, 255, 0.75)`, `rgba(15, 23, 42, 0.75)`, `radial-gradient(circle at top left, rgba(99,102,241,.15)`, `radial-gradient(circle at bottom right, rgba(168,85,247,.15)`, `#4f46e5`, `border-radius: 999px`
    - `frontend/src/index.css` contains `font-family: 'Inter'`
    - `cd frontend && npx tailwindcss --help` exits 0 (proves Tailwind v3 binary resolved)
  </acceptance_criteria>
  <done>Tailwind v3 config + PostCSS config + index.css authored with exact values from UX-UI reference; `.glass`, body gradient, and scrollbar styles are present.</done>
</task>

<task type="auto">
  <name>Task 3: Author frontend/index.html with anti-flash IIFE + CDN links</name>
  <files>frontend/index.html</files>
  <read_first>
    - .planning/phases/02-frontend-foundation/02-RESEARCH.md (Pattern 2 — Anti-Flash Dark Mode Script, Pitfall 3 — Dark Mode Flash, Pitfall 2 — Font Awesome icons)
    - .planning/phases/02-frontend-foundation/02-PATTERNS.md (frontend/index.html section)
    - .planning/phases/02-frontend-foundation/02-CONTEXT.md (D-04 FA CDN 6.5.1 exact URL, D-07 dark on html, D-08 default dark when no localStorage)
    - UX-UI/index.html (lines 1-77 — exact CDN URLs for FA and Inter Google Fonts)
    - frontend/index.html (the existing Vite scaffold file — replace its content; verify current state first)
  </read_first>
  <action>
    Replace the Vite-scaffolded `frontend/index.html` entirely. Set `<html lang="en">` (no `class="dark"` — the IIFE sets it). In `<head>`, set `<meta charset="UTF-8" />`, `<meta name="viewport" content="width=device-width, initial-scale=1.0" />`, and `<title>TaskFlow</title>` (app name per CONTEXT.md specifics). Immediately after `<title>`, place the anti-flash `<script>` block — it MUST be an IIFE that reads `localStorage.getItem('theme')`, then: if the value is the string `'light'` call `document.documentElement.classList.remove('dark')`, otherwise call `document.documentElement.classList.add('dark')` (default-dark per D-08, even when key is absent or null). This script must be plain (no `type="module"`, no `defer`, no `async`) so it runs synchronously during HTML parse BEFORE the React bundle is fetched — this is the critical ordering constraint from Pitfall 3. After the IIFE, add the Font Awesome 6.5.1 CDN `<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />` (exact URL per D-04, do NOT use any other version or CDN). Then add Google Fonts Inter: `<link rel="preconnect" href="https://fonts.googleapis.com">`, `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`, and `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet" />` (exact URL per UX-UI/index.html lines 31-37). In `<body>`, place `<div id="root"></div>` followed by `<script type="module" src="/src/main.jsx"></script>` — the module script MUST come AFTER the IIFE script in document order. Do NOT include the Tailwind Play CDN (`cdn.tailwindcss.com`) — that's the UX-UI prototype; the real app uses the npm-installed Tailwind v3 processed by PostCSS. Do NOT include SweetAlert2 — that is Phase 04.
  </action>
  <verify>
    <automated>cd frontend && python3 -c "import re; html=open('index.html').read(); iife=html.find('localStorage.getItem'); mod=html.find('src=\"/src/main.jsx\"'); fa=html.find('font-awesome/6.5.1/css/all.min.css'); inter=html.find('family=Inter:wght@100..900'); assert iife>=0 and mod>=0 and fa>=0 and inter>=0, 'missing required tags'; assert iife<mod, 'IIFE must precede module script'; assert iife<fa, 'IIFE must precede FA link (synchronous head order)'; assert 'cdn.tailwindcss.com' not in html, 'Play CDN must not be included'; assert 'sweetalert' not in html.lower(), 'SweetAlert2 belongs in Phase 04'; assert '<title>TaskFlow</title>' in html, 'title must be TaskFlow'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` `<html>` tag is `<html lang="en">` (no `class="dark"` literal — IIFE sets it dynamically)
    - File contains `<title>TaskFlow</title>` exactly
    - File contains a synchronous `<script>` (no `type="module"`, no `defer`, no `async` attribute on that tag) whose body calls `localStorage.getItem('theme')` and conditionally invokes `document.documentElement.classList.remove('dark')` for the literal string `'light'` and otherwise `document.documentElement.classList.add('dark')`
    - The byte offset of `localStorage.getItem('theme')` in the file is strictly less than the byte offset of `src="/src/main.jsx"` (IIFE before module script — Pitfall 3 / D-08)
    - File contains the literal href `https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css` (D-04 exact URL)
    - File contains the literal href substring `family=Inter:wght@100..900&display=swap`
    - File contains both `<link rel="preconnect" href="https://fonts.googleapis.com">` and `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`
    - File contains `<div id="root"></div>` and `<script type="module" src="/src/main.jsx"></script>`
    - File does NOT contain `cdn.tailwindcss.com` (Play CDN is prototype-only)
    - File does NOT contain `sweetalert` (case-insensitive — deferred to Phase 04)
    - File does NOT contain any `@fortawesome` reference
  </acceptance_criteria>
  <done>`frontend/index.html` boots with `dark` class set by IIFE before any React/Tailwind paint; FA + Inter loaded via CDN; module script wires to `/src/main.jsx`.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run dev` starts on http://localhost:5173 with no console errors.
2. Hard-refresh the page — body shows dual radial gradient (indigo top-left, purple bottom-right faint), and `<html>` has `class="dark"` (DevTools Elements panel).
3. No white flash on hard-refresh when `localStorage.theme` is unset or `'dark'`.
4. `npx tailwindcss --version` reports a `3.x.x` version (NOT `4.x.x`).
5. Network tab on initial load shows `font-awesome/6.5.1/css/all.min.css` and the Google Fonts Inter stylesheet both 200 OK.
6. No `@fortawesome/*` entries in `frontend/package.json`.
</verification>

<success_criteria>
- `frontend/` project exists, boots via `npm run dev`, serves on port 5173
- Tailwind v3 (not v4) installed and configured
- React Router DOM v6 (not v7) installed
- `index.html` contains the anti-flash IIFE BEFORE the module script tag
- Font Awesome 6.5.1 + Inter loaded via CDN only — no npm FA package
- `src/index.css` contains `.glass`, body radial gradient, `.sidebar-scroll`, and Inter font-family rule with exact values from `UX-UI/index.html`
- Dark mode is the default on first load (no localStorage entry)
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-foundation/02-01-SUMMARY.md` per the summary template.
</output>
