---
phase: "02-frontend-foundation"
plan: "02-03"
type: "execute"
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - "frontend/src/components/layout/Sidebar.jsx"
  - "frontend/src/components/layout/Topbar.jsx"
  - "frontend/src/components/ui/ThemeToggle.jsx"
  - "frontend/src/pages/Dashboard.jsx"
  - "frontend/src/pages/Tasks.jsx"
  - "frontend/src/pages/Analytics.jsx"
  - "frontend/src/pages/Settings.jsx"
  - "frontend/src/pages/ComingSoon.jsx"
autonomous: false
requirements: []
must_haves:
  truths:
    - "Sidebar renders six NavLink items in order: Dashboard, Tasks, Teams, Calendar, Analytics, Settings"
    - "Sidebar Dashboard NavLink uses the `end` prop so it is only active on exact `/` (Pitfall 5)"
    - "Sidebar in mobile mode (prop `mobile`) attaches `onClick={onClose}` to each NavLink so clicking a nav item closes the drawer (Pitfall 6)"
    - "Active NavLink className applies indigo→purple gradient background with white text"
    - "Sidebar desktop instance is `hidden xl:flex`; mobile instance is `flex`"
    - "Sidebar logo is the indigo→purple gradient rounded-2xl square containing `<i class='fa-solid fa-layer-group ...'>`"
    - "Sidebar bottom user card renders `https://i.pravatar.cc/100` avatar + 'Admin User' label"
    - "Topbar renders a `<button>` with the `xl:hidden` class that calls openDrawer() on click — the hamburger only appears below xl (D-01)"
    - "Topbar contains the search `<input>` with `readOnly` attribute (Phase 02 renders shell; Phase 03 wires search)"
    - "Topbar contains the bell button with the red notification dot (renders only — no dropdown per Phase 04 deferral)"
    - "Topbar title text changes based on current pathname via PAGE_TITLES map"
    - "ThemeToggle button calls useThemeStore().toggle and displays fa-sun when isDark or fa-moon when light"
    - "All five page stub files default-export a component rendering its respective heading text"
    - "ComingSoon accepts a `page` prop and renders the prop value as the heading"
  artifacts:
    - path: "frontend/src/components/layout/Sidebar.jsx"
      provides: "Desktop + mobile sidebar with six NavLinks, logo, scrollable nav, user card; accepts { mobile, onClose } props"
      min_lines: 50
    - path: "frontend/src/components/layout/Topbar.jsx"
      provides: "Sticky header with hamburger (xl:hidden), page title, search readOnly, bell, ThemeToggle"
      min_lines: 35
    - path: "frontend/src/components/ui/ThemeToggle.jsx"
      provides: "Theme toggle button using useThemeStore"
      min_lines: 10
    - path: "frontend/src/pages/Dashboard.jsx"
      provides: "Dashboard stub heading 'Dashboard Overview'"
      min_lines: 5
    - path: "frontend/src/pages/Tasks.jsx"
      provides: "Tasks stub heading 'Tasks'"
      min_lines: 5
    - path: "frontend/src/pages/Analytics.jsx"
      provides: "Analytics stub heading 'Analytics'"
      min_lines: 5
    - path: "frontend/src/pages/Settings.jsx"
      provides: "Settings stub heading 'Settings'"
      min_lines: 5
    - path: "frontend/src/pages/ComingSoon.jsx"
      provides: "Generic 'Coming Soon' stub accepting `page` prop"
      min_lines: 8
  key_links:
    - from: "frontend/src/components/layout/Sidebar.jsx"
      to: "react-router-dom NavLink"
      via: "import { NavLink } from 'react-router-dom'; NavLink with end prop on '/' route"
      pattern: "end=\\{to === '/'\\}"
    - from: "frontend/src/components/layout/Sidebar.jsx"
      to: "frontend/src/components/layout/Layout.jsx (mobile drawer)"
      via: "{ mobile, onClose } props; each NavLink calls onClose when mobile is true"
      pattern: "onClick=\\{mobile \\? onClose : undefined\\}"
    - from: "frontend/src/components/layout/Topbar.jsx"
      to: "frontend/src/stores/uiStore.js"
      via: "useUiStore() destructures openDrawer; hamburger button onClick={openDrawer}"
      pattern: "useUiStore\\(\\)"
    - from: "frontend/src/components/layout/Topbar.jsx"
      to: "frontend/src/components/ui/ThemeToggle.jsx"
      via: "import ThemeToggle and render in right cluster"
      pattern: "import ThemeToggle from"
    - from: "frontend/src/components/ui/ThemeToggle.jsx"
      to: "frontend/src/stores/themeStore.js"
      via: "useThemeStore() destructures isDark and toggle"
      pattern: "useThemeStore\\(\\)"
---

# Plan 02-03: Sidebar + Topbar + ThemeToggle + Page Stubs

## Objective

Author the visual layer that fills the `<Layout />` outlet wired in 02-02: the Sidebar (desktop + mobile drawer modes), the Topbar (with hamburger, page title, search shell, bell, ThemeToggle), the ThemeToggle button, and all five page stub components (Dashboard, Tasks, Analytics, Settings, ComingSoon).

**Purpose:** Close the loop — Plan 02-02 wired Routes and Layout but left the Sidebar/Topbar imports unresolved. This plan creates those components and the page stubs, producing a runnable, navigable, themed shell that satisfies the Phase 02 ROADMAP goal.

**Output:** A frontend where `npm run dev` boots, the dark glassmorphism shell renders pixel-accurate to `UX-UI/index.html`, navigating to `/`, `/tasks`, `/analytics`, `/settings`, `/teams`, `/calendar` updates page title + active nav highlight, the hamburger opens the drawer on mobile, the backdrop closes it, and the ThemeToggle flips themes with persistence.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-foundation/02-CONTEXT.md
@.planning/phases/02-frontend-foundation/02-RESEARCH.md
@.planning/phases/02-frontend-foundation/02-PATTERNS.md
@.planning/phases/02-frontend-foundation/02-01-SUMMARY.md
@.planning/phases/02-frontend-foundation/02-02-SUMMARY.md
@frontend/src/stores/themeStore.js
@frontend/src/stores/uiStore.js
@frontend/src/App.jsx
@frontend/src/components/layout/Layout.jsx
@UX-UI/index.html
</context>

<interfaces>
Contracts consumed from Plan 02-02:

useThemeStore (default-dark, toggle mutates DOM + localStorage):
- `const { isDark, toggle } = useThemeStore();`

useUiStore:
- `const { drawerOpen, openDrawer, closeDrawer } = useUiStore();`

Layout.jsx (already created in 02-02) imports `Sidebar` and `Topbar` from `./` relative — Task 1+2 below MUST honor those exact filenames: `frontend/src/components/layout/Sidebar.jsx` and `frontend/src/components/layout/Topbar.jsx`.

App.jsx (already created in 02-02) imports pages from `./pages/{Dashboard,Tasks,Analytics,Settings,ComingSoon}` — Task 4 MUST create those exact filenames.

Sidebar props contract (consumed by Layout.jsx mobile drawer):
- `mobile?: boolean` (default false) — when true, sidebar renders inside the mobile drawer wrapper
- `onClose?: () => void` — called by both the × button AND every NavLink onClick when `mobile === true`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement Sidebar.jsx (desktop + mobile, NavLink with `end` and onClose)</name>
  <files>frontend/src/components/layout/Sidebar.jsx</files>
  <read_first>
    - .planning/phases/02-frontend-foundation/02-RESEARCH.md (Pattern 5 — Sidebar Component; Pitfall 5 — NavLink end prop; Pitfall 6 — drawer close on mobile nav click; Open Questions #2 — active state white text)
    - .planning/phases/02-frontend-foundation/02-PATTERNS.md (Sidebar.jsx section — full navItems array, NavLink pattern with `end={to === '/'}` and `onClick={mobile ? onClose : undefined}`, logo, user card)
    - .planning/phases/02-frontend-foundation/02-CONTEXT.md (D-01 hidden xl:flex on desktop, D-02 × close button in mobile mode, specifics block — sidebar w-80, indigo→purple logo gradient, 'TaskFlow' app name, pravatar avatar, 'Admin User' label)
    - UX-UI/index.html (lines 84-220 — exact sidebar markup, class names, copy text)
    - frontend/src/components/layout/Layout.jsx (the consumer — confirm it passes `mobile` and `onClose` props)
  </read_first>
  <action>
    Create `frontend/src/components/layout/Sidebar.jsx`. Import `{ NavLink } from 'react-router-dom'`. Define a module-scoped `navItems` array containing exactly six objects IN THIS ORDER with these exact `to`/`icon`/`label`/`iconColor` values: (1) `{ to: '/', icon: 'fa-chart-pie', label: 'Dashboard' }` (no iconColor — Dashboard uses default white when active, default in light/dark when inactive), (2) `{ to: '/tasks', icon: 'fa-list-check', label: 'Tasks', iconColor: 'text-indigo-500' }`, (3) `{ to: '/teams', icon: 'fa-users', label: 'Teams', iconColor: 'text-pink-500' }`, (4) `{ to: '/calendar', icon: 'fa-calendar-days', label: 'Calendar', iconColor: 'text-green-500' }`, (5) `{ to: '/analytics', icon: 'fa-chart-line', label: 'Analytics', iconColor: 'text-orange-500' }`, (6) `{ to: '/settings', icon: 'fa-gear', label: 'Settings', iconColor: 'text-slate-500' }`. Default-export `Sidebar` accepting `{ mobile = false, onClose }` props. Return an `<aside>` with className computed by template literal: when `mobile` is true use `flex w-80 flex-col border-r border-white/20 dark:border-slate-800 glass h-full`; otherwise use `hidden xl:flex w-80 flex-col border-r border-white/20 dark:border-slate-800 glass h-full` (D-01). The `glass` class is from index.css. Inside `<aside>`: (A) Logo section `<div className="px-8 py-8 border-b border-white/20 dark:border-slate-800 flex items-center justify-between">` with a child `<div className="flex items-center gap-4">` containing the indigo→purple gradient square `<div className="w-14 h-14 rounded-2xl bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center shadow-lg shadow-indigo-500/30"><i className="fa-solid fa-layer-group text-white text-xl"></i></div>` and a `<div>` with `<h1 className="text-2xl font-black">TaskFlow</h1>` and `<p className="text-slate-500 dark:text-slate-400 text-sm">Enterprise Workspace</p>` (exact text from UX-UI line 99). When `mobile` is true, render a sibling close button `<button onClick={onClose} aria-label="Close menu" className="text-slate-400 hover:text-white text-2xl">×</button>` inside the logo section's outer flex (uses the HTML entity `&times;`). (B) Nav section `<div className="flex-1 overflow-y-auto sidebar-scroll px-5 py-6">` with `<p className="text-xs uppercase tracking-widest text-slate-400 mb-4 px-4">Main Menu</p>` and `<nav className="space-y-2">` containing `navItems.map(...)`. Each map iteration renders a `<NavLink key={to} to={to} end={to === '/'} onClick={mobile ? onClose : undefined} className={({isActive}) => isActive ? <active classes> : <inactive classes>}>`. The `end={to === '/'}` prop is REQUIRED on the Dashboard NavLink to fix Pitfall 5 (NavLink is prefix-match by default; without `end`, Dashboard would always appear active). The `onClick={mobile ? onClose : undefined}` is REQUIRED to fix Pitfall 6. Active className string: `flex items-center gap-4 px-5 py-4 rounded-2xl bg-gradient-to-r from-indigo-600 to-purple-600 text-white shadow-xl shadow-indigo-500/20`. Inactive className string: `flex items-center gap-4 px-5 py-4 rounded-2xl hover:bg-white/70 dark:hover:bg-slate-800 transition`. Inside the NavLink render `<i className={'fa-solid ' + icon + (isActive ? '' : ' ' + (iconColor || ''))}></i>` and `{label}` — the `isActive` discriminator can be returned from a render-prop child of NavLink or use a wrapper function; PATTERNS.md uses the child render-prop form `{({isActive}) => (...)}` returning a fragment. Per Open Questions #2 — active items use ALL white text/icon (no individual iconColor), inactive items use the iconColor class. (C) User card section at the bottom `<div className="p-5 border-t border-white/20 dark:border-slate-800">` with `<div className="flex items-center gap-4 p-4 rounded-2xl bg-white/60 dark:bg-slate-900">` containing `<img src="https://i.pravatar.cc/100" className="w-14 h-14 rounded-2xl object-cover" alt="Admin" />`, `<div className="flex-1"><h3 className="font-bold">Admin User</h3><p className="text-sm text-slate-500">Full Stack Developer</p></div>`, and a logout button `<button className="w-10 h-10 rounded-xl bg-slate-100 dark:bg-slate-800"><i className="fa-solid fa-right-from-bracket"></i></button>`. Do NOT wire the logout button click (Phase 04 deferral). Do NOT use `<a>` tags — only `<NavLink>` (Anti-Pattern).
  </action>
  <verify>
    <automated>cd frontend && grep -q "import { NavLink } from 'react-router-dom'" src/components/layout/Sidebar.jsx && grep -q "to: '/', icon: 'fa-chart-pie', label: 'Dashboard'" src/components/layout/Sidebar.jsx && grep -q "to: '/tasks'" src/components/layout/Sidebar.jsx && grep -q "to: '/teams'" src/components/layout/Sidebar.jsx && grep -q "to: '/calendar'" src/components/layout/Sidebar.jsx && grep -q "to: '/analytics'" src/components/layout/Sidebar.jsx && grep -q "to: '/settings'" src/components/layout/Sidebar.jsx && grep -q "fa-list-check" src/components/layout/Sidebar.jsx && grep -q "fa-users" src/components/layout/Sidebar.jsx && grep -q "fa-calendar-days" src/components/layout/Sidebar.jsx && grep -q "fa-chart-line" src/components/layout/Sidebar.jsx && grep -q "fa-gear" src/components/layout/Sidebar.jsx && grep -q "end={to === '/'}" src/components/layout/Sidebar.jsx && grep -q "onClick={mobile ? onClose : undefined}" src/components/layout/Sidebar.jsx && grep -q "hidden xl:flex" src/components/layout/Sidebar.jsx && grep -q "from-indigo-600 to-purple-600" src/components/layout/Sidebar.jsx && grep -q "fa-layer-group" src/components/layout/Sidebar.jsx && grep -q "TaskFlow" src/components/layout/Sidebar.jsx && grep -q "Enterprise Workspace" src/components/layout/Sidebar.jsx && grep -q "i.pravatar.cc/100" src/components/layout/Sidebar.jsx && grep -q "Admin User" src/components/layout/Sidebar.jsx && grep -q "Main Menu" src/components/layout/Sidebar.jsx && grep -q "sidebar-scroll" src/components/layout/Sidebar.jsx && grep -q "fa-right-from-bracket" src/components/layout/Sidebar.jsx && ! grep -q "<a href" src/components/layout/Sidebar.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File imports `NavLink` from `react-router-dom` (exact)
    - File declares an array containing exactly six entries with `to` values in order: `/`, `/tasks`, `/teams`, `/calendar`, `/analytics`, `/settings`
    - File contains literal `end={to === '/'}` (Pitfall 5 fix)
    - File contains literal `onClick={mobile ? onClose : undefined}` (Pitfall 6 fix)
    - File contains literal class substring `hidden xl:flex` (desktop visibility) AND a branch for mobile that omits `hidden xl:flex` (mobile renders unconditionally)
    - File contains literal `bg-gradient-to-r from-indigo-600 to-purple-600 text-white` (active NavLink style)
    - File contains literal `hover:bg-white/70 dark:hover:bg-slate-800` (inactive NavLink hover)
    - File contains literal `bg-gradient-to-br from-indigo-600 to-purple-600` (logo gradient — note `to-br` direction)
    - File contains literal `fa-layer-group` (logo icon)
    - File contains literal text `TaskFlow` and `Enterprise Workspace` and `Admin User` and `Full Stack Developer` and `Main Menu`
    - File contains literal `https://i.pravatar.cc/100`
    - File contains literal `sidebar-scroll` (custom scrollbar class)
    - File contains literal `fa-right-from-bracket` (logout icon)
    - File contains a `<button onClick={onClose}` (the mobile × close button) AND that button is conditionally rendered based on `mobile` (file should have a `mobile &&` JSX conditional before the close button)
    - File does NOT contain `<a href` (uses NavLink, not anchor)
  </acceptance_criteria>
  <done>Sidebar.jsx renders six NavLinks with `end` and mobile-onClose-close behavior, logo, user card — matches UX-UI/index.html structure exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Topbar.jsx + ThemeToggle.jsx</name>
  <files>frontend/src/components/layout/Topbar.jsx, frontend/src/components/ui/ThemeToggle.jsx</files>
  <read_first>
    - .planning/phases/02-frontend-foundation/02-RESEARCH.md (Pattern 6 — Topbar with Hamburger; Pattern 7 — ThemeToggle)
    - .planning/phases/02-frontend-foundation/02-PATTERNS.md (Topbar.jsx and ThemeToggle.jsx sections)
    - .planning/phases/02-frontend-foundation/02-CONTEXT.md (D-01 hamburger appears below xl, deferred ideas — search renders but not wired, bell renders but no dropdown)
    - UX-UI/index.html (the topbar section — exact class names for search, bell, theme button)
    - frontend/src/stores/uiStore.js (consumed by Topbar for openDrawer)
    - frontend/src/stores/themeStore.js (consumed by ThemeToggle for isDark + toggle)
  </read_first>
  <action>
    Create `frontend/src/components/ui/ThemeToggle.jsx`. Import `{ useThemeStore } from '../../stores/themeStore'`. Default-export `ThemeToggle`. Inside, destructure `{ isDark, toggle } = useThemeStore()`. Return `<button onClick={toggle} aria-label="Toggle theme" className="w-14 h-14 rounded-2xl bg-gradient-to-br from-indigo-600 to-purple-600 text-white shadow-lg shadow-indigo-500/20"><i className={\`fa-solid ${isDark ? 'fa-sun' : 'fa-moon'}\`}></i></button>`. Icon logic: `fa-sun` when currently dark (click will switch to light), `fa-moon` when currently light. Create `frontend/src/components/layout/Topbar.jsx`. Imports: `import { useLocation } from 'react-router-dom'`, `import { useUiStore } from '../../stores/uiStore'`, `import ThemeToggle from '../ui/ThemeToggle'`. Define module-scoped object `PAGE_TITLES` mapping pathnames to title strings: `{ '/': 'Dashboard Overview', '/tasks': 'Tasks', '/analytics': 'Analytics', '/settings': 'Settings', '/teams': 'Teams', '/calendar': 'Calendar' }`. Default-export `Topbar`. Inside, destructure `{ openDrawer } = useUiStore()` and `{ pathname } = useLocation()`. Compute `const title = PAGE_TITLES[pathname] ?? 'TaskFlow'`. Return a `<header className="sticky top-0 z-50 border-b border-white/20 dark:border-slate-800 glass">` containing a `<div className="px-8 py-5 flex items-center justify-between">` with TWO child clusters: LEFT cluster `<div className="flex items-center gap-4">` containing (a) hamburger button `<button className="xl:hidden w-10 h-10 rounded-xl bg-white/80 dark:bg-slate-900 border border-white/20 dark:border-slate-700" onClick={openDrawer} aria-label="Open menu"><i className="fa-solid fa-bars"></i></button>` — the `xl:hidden` class ensures it appears ONLY below the xl breakpoint per D-01 — and (b) title block `<div><h2 className="text-3xl font-black">{title}</h2><p className="text-slate-500 mt-1">Welcome back here&apos;s what&apos;s happening today.</p></div>`. RIGHT cluster `<div className="flex items-center gap-4">` containing (c) search input wrapper `<div className="relative hidden lg:block"><input type="text" placeholder="Search tasks, projects..." className="w-96 pl-12 pr-5 py-4 rounded-2xl bg-white/80 dark:bg-slate-900 border border-white/20 dark:border-slate-700 outline-none focus:ring-2 focus:ring-indigo-500" readOnly /><i className="fa-solid fa-magnifying-glass absolute left-5 top-5 text-slate-400"></i></div>` — the `readOnly` attribute is REQUIRED (search is shell-only in Phase 02, wired in Phase 03), (d) bell `<button className="relative w-14 h-14 rounded-2xl bg-white/80 dark:bg-slate-900 border border-white/20 dark:border-slate-700"><i className="fa-solid fa-bell"></i><span className="absolute top-3 right-3 w-3 h-3 rounded-full bg-red-500"></span></button>` — no onClick (dropdown deferred to Phase 04), and (e) `<ThemeToggle />`. Do NOT add an `onClick` to the search input wrapper. Do NOT add an `onClick` to the bell button.
  </action>
  <verify>
    <automated>cd frontend && grep -q "import { useThemeStore } from '../../stores/themeStore'" src/components/ui/ThemeToggle.jsx && grep -q "useThemeStore()" src/components/ui/ThemeToggle.jsx && grep -q "onClick={toggle}" src/components/ui/ThemeToggle.jsx && grep -q "isDark ? 'fa-sun' : 'fa-moon'" src/components/ui/ThemeToggle.jsx && grep -q "from-indigo-600 to-purple-600" src/components/ui/ThemeToggle.jsx && grep -q "import { useLocation } from 'react-router-dom'" src/components/layout/Topbar.jsx && grep -q "import { useUiStore } from '../../stores/uiStore'" src/components/layout/Topbar.jsx && grep -q "import ThemeToggle from '../ui/ThemeToggle'" src/components/layout/Topbar.jsx && grep -q "PAGE_TITLES" src/components/layout/Topbar.jsx && grep -q "'/': 'Dashboard Overview'" src/components/layout/Topbar.jsx && grep -q "'/tasks': 'Tasks'" src/components/layout/Topbar.jsx && grep -q "'/analytics': 'Analytics'" src/components/layout/Topbar.jsx && grep -q "'/settings': 'Settings'" src/components/layout/Topbar.jsx && grep -q "'/teams': 'Teams'" src/components/layout/Topbar.jsx && grep -q "'/calendar': 'Calendar'" src/components/layout/Topbar.jsx && grep -q "onClick={openDrawer}" src/components/layout/Topbar.jsx && grep -q "xl:hidden" src/components/layout/Topbar.jsx && grep -q "fa-bars" src/components/layout/Topbar.jsx && grep -q "fa-bell" src/components/layout/Topbar.jsx && grep -q "fa-magnifying-glass" src/components/layout/Topbar.jsx && grep -q "readOnly" src/components/layout/Topbar.jsx && grep -q "sticky top-0" src/components/layout/Topbar.jsx && grep -q "<ThemeToggle />" src/components/layout/Topbar.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/ui/ThemeToggle.jsx` default-exports a component whose `<button>` has `onClick={toggle}` and `aria-label="Toggle theme"`
    - ThemeToggle uses the conditional `isDark ? 'fa-sun' : 'fa-moon'` literally
    - ThemeToggle button className contains `bg-gradient-to-br from-indigo-600 to-purple-600`
    - `frontend/src/components/layout/Topbar.jsx` imports `useLocation` from `react-router-dom` and `useUiStore` from the stores path and `ThemeToggle` from the UI path (exact relative paths)
    - Topbar declares a `PAGE_TITLES` object with all six pathname keys mapping to their title strings
    - Topbar uses `PAGE_TITLES[pathname] ?? 'TaskFlow'` (nullish coalescing fallback)
    - Topbar contains a `<button ... className="xl:hidden ..." onClick={openDrawer}` (hamburger appears only below xl — D-01)
    - Topbar contains `<i className="fa-solid fa-bars">` (hamburger icon)
    - Topbar contains `<input ... readOnly` (search shell — deferred to Phase 03)
    - Topbar contains the bell button with `<i className="fa-solid fa-bell">` AND a red dot span (`bg-red-500`); the bell button has NO `onClick` handler
    - Topbar `<header>` has `sticky top-0 z-50` (sticks under scroll) and `glass` class
    - Topbar renders `<ThemeToggle />` literally
  </acceptance_criteria>
  <done>ThemeToggle calls useThemeStore.toggle; Topbar wires hamburger→openDrawer, shows dynamic title, and renders search shell + bell shell + ThemeToggle.</done>
</task>

<task type="auto">
  <name>Task 3: Create page stubs (Dashboard, Tasks, Analytics, Settings, ComingSoon)</name>
  <files>frontend/src/pages/Dashboard.jsx, frontend/src/pages/Tasks.jsx, frontend/src/pages/Analytics.jsx, frontend/src/pages/Settings.jsx, frontend/src/pages/ComingSoon.jsx</files>
  <read_first>
    - .planning/phases/02-frontend-foundation/02-PATTERNS.md (Page stubs and ComingSoon sections — exact patterns)
    - .planning/phases/02-frontend-foundation/02-RESEARCH.md (Recommended Project Structure — pages directory)
    - .planning/phases/02-frontend-foundation/02-CONTEXT.md (deferred — Teams and Calendar are Phase 04, render ComingSoon stub here)
    - frontend/src/App.jsx (the consumer — confirm import paths match: ./pages/Dashboard, ./pages/Tasks, ./pages/Analytics, ./pages/Settings, ./pages/ComingSoon)
  </read_first>
  <action>
    Create five files in `frontend/src/pages/`. Each is a stub — Phase 03 fills in full content. Pattern for each named page: default-export a component returning `<div><h2 className="text-2xl font-bold">{heading}</h2></div>`. Specific headings: `Dashboard.jsx` → `Dashboard Overview`, `Tasks.jsx` → `Tasks`, `Analytics.jsx` → `Analytics`, `Settings.jsx` → `Settings`. For `ComingSoon.jsx`, default-export a component accepting `{ page = 'Page' }` props that returns `<div className="flex flex-col items-center justify-center h-64 text-slate-400"><i className="fa-solid fa-clock text-4xl mb-4"></i><h2 className="text-2xl font-bold">{page}</h2><p className="mt-2">Coming Soon</p></div>` — the `page` prop is supplied by App.jsx routes (`/teams` passes "Teams", `/calendar` passes "Calendar"). Do NOT add real content (charts, tables, forms) — these are Phase 03 scope. Do NOT add useEffect or state — these are pure stubs.
  </action>
  <verify>
    <automated>cd frontend && grep -q "Dashboard Overview" src/pages/Dashboard.jsx && grep -q "export default function Dashboard" src/pages/Dashboard.jsx && grep -q "export default function Tasks" src/pages/Tasks.jsx && grep -q "export default function Analytics" src/pages/Analytics.jsx && grep -q "export default function Settings" src/pages/Settings.jsx && grep -q "export default function ComingSoon" src/pages/ComingSoon.jsx && grep -q "page = 'Page'" src/pages/ComingSoon.jsx && grep -q "fa-clock" src/pages/ComingSoon.jsx && grep -q "Coming Soon" src/pages/ComingSoon.jsx && ! grep -q "useState" src/pages/Dashboard.jsx && ! grep -q "useEffect" src/pages/Dashboard.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Dashboard.jsx` default-exports `Dashboard` returning JSX containing literal text `Dashboard Overview`
    - `frontend/src/pages/Tasks.jsx` default-exports `Tasks` returning JSX containing literal text `Tasks`
    - `frontend/src/pages/Analytics.jsx` default-exports `Analytics` returning JSX containing literal text `Analytics`
    - `frontend/src/pages/Settings.jsx` default-exports `Settings` returning JSX containing literal text `Settings`
    - `frontend/src/pages/ComingSoon.jsx` default-exports `ComingSoon` whose signature is `function ComingSoon({ page = 'Page' })`
    - ComingSoon JSX contains `fa-clock` icon AND the literal text `Coming Soon` AND renders `{page}` as the heading text
    - None of the stub files import `useState` or `useEffect` (pure stubs only)
  </acceptance_criteria>
  <done>All five page components exist and resolve App.jsx's imports — `npm run dev` boots without unresolved-import errors.</done>
</task>

<task type="auto">
  <name>Task 4: Final wire-up verification — production build + visual smoke</name>
  <files>frontend/src/components/layout/Sidebar.jsx, frontend/src/components/layout/Topbar.jsx, frontend/src/components/ui/ThemeToggle.jsx, frontend/src/pages/Dashboard.jsx, frontend/src/pages/Tasks.jsx, frontend/src/pages/Analytics.jsx, frontend/src/pages/Settings.jsx, frontend/src/pages/ComingSoon.jsx</files>
  <read_first>
    - .planning/phases/02-frontend-foundation/02-RESEARCH.md (Validation Architecture — 5-check phase gate)
    - frontend/package.json (verify `scripts.build` exists from Vite scaffold)
    - frontend/src/App.jsx (verify all imports resolve)
    - frontend/src/components/layout/Layout.jsx (verify Sidebar/Topbar imports resolve)
  </read_first>
  <action>
    Run a final integration sanity check on the wired system. Execute `cd frontend && npm run build` — Vite must complete the production build with exit code 0. If the build fails due to a syntax or import error in any of the files from Tasks 1-3 or Plan 02-02, fix the offending file in place (this task's `files_modified` lists all eligible targets). Do NOT add new features during the fix — only repair the existing wire-up. After the build succeeds, run `cd frontend && npm run dev -- --port 5173` briefly (background, then terminate) and `curl -sf http://localhost:5173/ -o /dev/null` to confirm the index document serves. The dev server must respond 200 OK with the `<div id="root"></div>` in the served HTML. Do NOT add a new file; this task only validates and patches.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tee /tmp/02-03-build.log | tail -5 && test -d dist && test -f dist/index.html && grep -q '<div id="root">' dist/index.html && grep -q 'font-awesome/6.5.1' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 (production build succeeds — all imports resolve, no syntax errors)
    - `frontend/dist/index.html` exists after the build
    - `frontend/dist/index.html` contains `<div id="root">` (the React mount point survives the build)
    - `frontend/dist/index.html` contains the FA 6.5.1 CDN href (the CDN link is preserved through the build)
    - `frontend/dist/assets/` contains at least one bundled `.js` file and one bundled `.css` file (verifies Tailwind directives compiled — `ls frontend/dist/assets/*.css` finds at least one match)
  </acceptance_criteria>
  <done>Production build completes; all component wiring resolves; bundled CSS contains Tailwind output; app is ready for human UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human visual UAT (5-check phase gate)</name>
  <what-built>Full Phase 02 shell: Vite + React + Tailwind v3 frontend with anti-flash dark mode, Zustand stores (theme + ui), React Router v6 with Layout outlet, Sidebar (desktop + mobile drawer), Topbar (hamburger + search shell + bell + ThemeToggle), and page stubs for /, /tasks, /analytics, /settings, /teams, /calendar.</what-built>
  <how-to-verify>
    Run `cd frontend && npm run dev` and open `http://localhost:5173` in a browser. Perform the five RESEARCH.md Validation Architecture checks:
    1. **Visual check (desktop ≥ xl):** App loads in dark mode. Sidebar visible on the left (w-80, glass effect, indigo→purple gradient logo, six nav items). Topbar at the top. Dual-radial gradient body background.
    2. **Route check:** Click each sidebar nav item — Dashboard, Tasks, Teams, Calendar, Analytics, Settings. URL updates. Topbar title updates per PAGE_TITLES map. Active nav item shows indigo→purple gradient with white text. Dashboard is highlighted ONLY when URL is exactly `/` (not when on any other path — Pitfall 5 fix verified).
    3. **Mobile check (resize browser below xl breakpoint, ~1280px):** Sidebar disappears from layout. Hamburger button appears in the topbar. Click hamburger — drawer slides in from the left with the same sidebar contents PLUS a `×` close button. Click outside the drawer (the dimmed backdrop) — drawer slides out. Re-open drawer, click any nav item — drawer closes AND the URL navigates (Pitfall 6 fix verified).
    4. **Theme check:** Click the ThemeToggle (gradient button with sun/moon icon). Theme flips to light. Hard-refresh the page — light mode persists (no flash to dark). Click again → dark. Hard-refresh in dark mode → no white flash on load.
    5. **Icon check:** Every Font Awesome icon renders correctly (no empty boxes): logo (`fa-layer-group`), nav items (`fa-chart-pie`, `fa-list-check`, `fa-users`, `fa-calendar-days`, `fa-chart-line`, `fa-gear`), topbar (`fa-bars`, `fa-magnifying-glass`, `fa-bell`), theme toggle (`fa-sun` or `fa-moon`), logout (`fa-right-from-bracket`), ComingSoon (`fa-clock`).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass. Otherwise describe which check failed and what was observed.</resume-signal>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` exits 0 (verified by Task 4).
2. The five RESEARCH.md Validation Architecture checks all pass in the browser (verified by Task 5).
3. `frontend/dist/index.html` retains the anti-flash IIFE and CDN links after the production build.
4. No console errors during dev or build (no unresolved imports, no missing files).
</verification>

<success_criteria>
- Sidebar.jsx renders six NavLinks with `end` prop on `/` (Pitfall 5) and `onClick={mobile ? onClose : undefined}` (Pitfall 6)
- Topbar.jsx wires `onClick={openDrawer}` on the `xl:hidden` hamburger and renders ThemeToggle
- ThemeToggle.jsx toggles theme via useThemeStore — sun in dark, moon in light
- Page stubs exist for Dashboard, Tasks, Analytics, Settings, and the generic ComingSoon
- `npm run build` completes successfully
- Human UAT passes all five RESEARCH.md Validation checks
- Phase 02 ROADMAP goal achieved: "Vite + React app with layout system, sidebar, topbar, routing, and theme toggle wired up"
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-foundation/02-03-SUMMARY.md` per the summary template.
</output>
